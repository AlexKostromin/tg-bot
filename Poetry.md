# 📦 Poetry - Управление зависимостями проекта

Poetry - это современный инструмент управления зависимостями и упаковки для Python. Он заменяет `requirements.txt` на `pyproject.toml` и обеспечивает полный контроль над версиями.

## 📋 Содержание
- [Установка Poetry](#установка-poetry)
- [Активация окружения](#активация-окружения)
- [Управление зависимостями](#управление-зависимостями)
- [Команды Poetry](#команды-poetry)
- [Структура проекта](#структура-проекта)

---

## 🔧 Установка Poetry

### На Linux/macOS:

```bash
curl -sSL https://install.python-poetry.org | python3 -
```

После установки добавьте Poetry в PATH:

```bash
export PATH="$HOME/.local/bin:$PATH"
```

Для постоянного добавления в PATH добавьте строку в `~/.bashrc` или `~/.zshrc`.

### На Windows (PowerShell):

```powershell
(Invoke-WebRequest -Uri https://install.python-poetry.org -UseBasicParsing).Content | python -
```

### Проверка установки:

```bash
poetry --version
```

---

## 🚀 Активация окружения

### Способ 1: Автоматическая активация (рекомендуется)

Poetry автоматически создает и управляет виртуальным окружением.

```bash
# 1. Перейдите в корневую папку проекта
cd /path/to/tg-bot

# 2. Установите зависимости (создаст виртуальное окружение)
poetry install

# 3. Активируйте оболочку Poetry
poetry shell

# Теперь вы находитесь в виртуальном окружении
# Используйте команды как обычно: python, pip, django-admin и т.д.
```

### Способ 2: Запуск команд в окружении (без активации)

```bash
# Запустить Python скрипт
poetry run python script.py

# Запустить Django команды
poetry run python manage.py migrate

# Запустить тесты
poetry run pytest
```

### Способ 3: Ручная активация виртуального окружения

Если вы предпочитаете работать с виртуальным окружением напрямую:

```bash
# Узнайте путь к виртуальному окружению
poetry env info --path

# Активируйте его вручную (Linux/macOS)
source $(poetry env info --path)/bin/activate

# Или на Windows
& "$(poetry env info --path)\Scripts\Activate.ps1"
```

### Выход из окружения:

```bash
# Если вы использовали "poetry shell"
exit

# Или если вы активировали вручную
deactivate
```

---

## 📚 Управление зависимостями

### Добавление новой зависимости:

```bash
# Добавить в основные зависимости
poetry add package_name

# Добавить с конкретной версией
poetry add package_name==1.2.3

# Добавить в dev зависимости (для разработки)
poetry add --group dev package_name
```

### Удаление зависимости:

```bash
poetry remove package_name
```

### Обновление зависимостей:

```bash
# Обновить все зависимости
poetry update

# Обновить конкретную зависимость
poetry update package_name
```

### Просмотр зависимостей:

```bash
# Показать дерево зависимостей
poetry show --tree

# Показать только top-level зависимости
poetry show
```

---

## 🎯 Команды Poetry

### Основные команды:

```bash
# Инициализация нового проекта
poetry init

# Установить зависимости из pyproject.toml и poetry.lock
poetry install

# Добавить новую зависимость
poetry add django

# Удалить зависимость
poetry remove django

# Обновить зависимости
poetry update

# Активировать виртуальное окружение
poetry shell

# Запустить команду в окружении
poetry run python manage.py runserver

# Показать информацию об окружении
poetry env info

# Список виртуальных окружений проекта
poetry env list

# Удалить виртуальное окружение
poetry env remove python3.10
```

### Команды для разработки:

```bash
# Установить dev зависимости
poetry install --with dev

# Форматирование кода (Black)
poetry run black .

# Проверка стиля кода (flake8)
poetry run flake8 .

# Сортировка импортов (isort)
poetry run isort .

# Type checking (mypy)
poetry run mypy backend

# Запуск тестов (pytest)
poetry run pytest
```

---

## 📁 Структура проекта

```
tg-bot/
├── pyproject.toml          # Конфигурация Poetry и метаданные проекта
├── poetry.lock             # Заблокированные версии зависимостей (автогенерируется)
├── Poetry.md              # Этот файл
├── backend/
│   ├── manage.py
│   ├── config/
│   ├── apps/
│   │   ├── bot/
│   │   ├── users/
│   │   └── competitions/
│   └── requirements.txt    # Старый формат (используйте pyproject.toml)
└── docker-compose.yml
```

---

## 🔄 Миграция с requirements.txt на Poetry

Если у вас есть `requirements.txt`, Poetry может автоматически его конвертировать:

```bash
# Poetry автоматически прочитает requirements.txt при инициализации
poetry install

# Удалите старый requirements.txt, если больше не нужен
rm backend/requirements.txt
```

---

## 📝 Файл pyproject.toml

### Основные секции:

```toml
[tool.poetry]
name = "tg-bot"                    # Название проекта
version = "0.1.0"                  # Версия
description = "..."                # Описание
authors = ["Your Name <email>"]    # Авторы

[tool.poetry.dependencies]         # Основные зависимости
python = "^3.10"                   # Python 3.10 или выше
Django = "^4.2"                    # Django 4.2 или совместимая версия

[tool.poetry.group.dev.dependencies]  # Dev зависимости
pytest = "^7.4.0"

[build-system]                     # Система сборки
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
```

---

## ✅ Проверка готовности окружения

После установки проверьте, что всё работает:

```bash
# Активируйте окружение
poetry shell

# Проверьте Python
python --version

# Проверьте Django
python -c "import django; print(django.__version__)"

# Проверьте Telegram Bot
python -c "import telegram; print(telegram.__version__)"

# Запустите миграции
python manage.py migrate

# Запустите тесты (если есть)
pytest

# Выйдите из окружения
exit
```

---

## 🐳 Poetry с Docker

В Docker Compose проект уже настроен для работы с Poetry:

```bash
# Запустить контейнер
docker-compose up -d

# Выполнить команды в контейнере
docker-compose exec backend poetry install
docker-compose exec backend poetry run python manage.py migrate
```

---

## 📚 Полезные ссылки

- [Официальная документация Poetry](https://python-poetry.org/docs/)
- [Сравнение Poetry и pip](https://python-poetry.org/docs/dependency-specification/)
- [pyproject.toml спецификация](https://www.python-org/dev/peps/pep-0517/)

---

## 💡 Советы и трюки

### Использование разных версий Python:

```bash
# Проверить установленные версии Python
poetry env list

# Использовать конкретную версию Python
poetry env use /usr/bin/python3.10

# Пересоздать окружение с новой версией
poetry env remove python3.10
poetry install
```

### Экспорт requirements.txt:

Если вам нужен файл `requirements.txt` для совместимости:

```bash
poetry export -f requirements.txt --output requirements.txt
```

### Отключение создания виртуального окружения:

```bash
poetry config virtualenvs.create false
```

---

## ❓ Часто задаваемые вопросы

**Q: Как мне обновить зависимости?**
A: Используйте `poetry update` для обновления всех зависимостей или `poetry add package@latest` для конкретной.

**Q: Почему файл poetry.lock так большой?**
A: poetry.lock хранит точные версии всех зависимостей и их подзависимостей для воспроизводимых установок.

**Q: Могу ли я удалить poetry.lock?**
A: Не рекомендуется. Этот файл обеспечивает консистентность между разработчиками и средами.

**Q: Как работать в IDE с Poetry?**
A: Укажите интерпретатор Python, которого использует Poetry. Обычно `$(poetry env info --path)/bin/python`

---

**Последнее обновление:** февраль 2026
**Версия Poetry:** 1.7.0+